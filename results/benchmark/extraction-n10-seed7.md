# Extraction benchmark `n10-seed7`: 10 unit processes with a report, seed 7

The whole route (locate → evidence → extract → map → assemble → resolve → calibrate → build → check) on unit processes whose report is in the bundle, scored against their real exchanges.

| status | n |
|---|---|
| scored | 9 |
| pages-not-found | 1 |

Scored cases: 9. Inputs: recall 69%, precision 85%, amounts within ±20 % 43/47 of matched; direct flows: recall 53%; rebuilt inventory: flows within ±10 % median 77 %, median |Δ flow| median 2.2 %.
