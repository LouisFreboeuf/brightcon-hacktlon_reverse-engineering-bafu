# Extraction benchmark `cem2bll`: 1 unit processes with a report, seed 7

The whole route (locate → evidence → extract → map → assemble → resolve → calibrate → build → check) on unit processes whose report is in the bundle, scored against their real exchanges.

| status | n |
|---|---|
| scored | 1 |

Scored cases: 1. Inputs: recall 100%, precision 100%, amounts within ±20 % 8/9 of matched; direct flows: recall 100%; climate |Δ| median 0.6 %; categories within ±10 % median 24/25.
