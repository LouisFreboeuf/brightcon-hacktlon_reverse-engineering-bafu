# Extraction benchmark `n100-seed7`: 100 unit processes with a report, seed 7

The whole route (locate → evidence → extract → map → assemble → resolve → calibrate → build → check) on unit processes whose report is in the bundle, scored against their real exchanges.

| status | n |
|---|---|
| scored | 58 |
| pages-not-found | 42 |

Scored cases: 58. Inputs: recall 56%, precision 71%, amounts within ±20 % 233/262 of matched; direct flows: recall 45%; rebuilt inventory: flows within ±10 % median 76 %, median |Δ flow| median 2.2 %.
